---
import Layout from '../layouts/Layout.astro';
import Ruleta from '../components/Ruleta.astro'; // 1. IMPORTAMOS LA RULETA EN BETA
---

<Layout showLevelButtons={true}>
  <!-- MODAL DE BIENVENIDA Y REGLAS - MODO BETA (NUEVO) -->
  <div id="rules-modal" class="modal-overlay">
    <div class="modal-card animate-pop">
      <div class="modal-header">
        <span class="modal-icon">🚀</span>
        <h2>¡Bienvenidos al Nivel Beta!</h2>
      </div>
      <div class="modal-body">
        <p class="modal-subtitle">¡La dificultad aumenta en el espacio! Sigue las reglas:</p>
        <ul class="rules-list">
          <li><span>⚡</span> Tienes solo 15 segundos para resolver operaciones de multiplicación y división.</li>
          <li><span>🎯</span> Si respondes bien, desbloquearás la ruleta espacial para avanzar.</li>
          <li><span>🛸</span> Si el tiempo se agota o fallas, el turno pasará al siguiente equipo automáticamente. ¡Sé veloz!</li>
        </ul>
      </div>
      <button type="button" id="btn-close-rules" class="modal-btn">
        ¡Iniciar Misión! 🎲
      </button>
    </div>
  </div>

  <!-- CONTENEDOR PRINCIPAL -->
  <div class="game-container">
    <div class="header-beta">
      <h1 class="level-title">Nivel Beta</h1>
      <div class="timer-box">
        <span id="time-left">15</span>s
      </div>
    </div>

    <div class="screen">
      <h2 id="math-question">Cargando...</h2>
    </div>

    <div class="fruits-container" id="options-container"></div>

    <p id="feedback-message" class="feedback"></p>

    <Ruleta />
  </div>
</Layout>

<style>
  /* --- NUEVOS ESTILOS DEL MODAL ANIMADO EN MODO BETA --- */
  .modal-overlay {
    position: fixed;
    top: 0;
    left: 0;
    width: 100vw;
    height: 100vh;
    background: rgba(10, 15, 30, 0.9); /* Un tono más oscuro y espacial */
    backdrop-filter: blur(8px);
    display: flex;
    justify-content: center;
    align-items: center;
    z-index: 10000;
    transition: opacity 0.4s ease, visibility 0.4s ease;
  }

  .modal-card {
    background: #ffffff;
    width: 90%;
    max-width: 500px;
    padding: 2.5rem;
    border-radius: 32px;
    box-shadow: 0 20px 50px rgba(0, 0, 0, 0.6);
    text-align: center;
    box-sizing: border-box;
    border: 3px solid #3B82F6; /* Borde Azul característico de Beta */
  }

  .modal-header {
    margin-bottom: 1.5rem;
  }

  .modal-icon {
    font-size: 3.5rem;
    display: block;
    margin-bottom: 0.5rem;
  }

  .modal-card h2 {
    font-size: 2rem;
    color: #1e3a5f;
    margin: 0;
    font-family: 'Fredoka', sans-serif;
  }

  .modal-subtitle {
    font-size: 1.1rem;
    color: #64748b;
    margin-bottom: 1.5rem;
  }

  .rules-list {
    list-style: none;
    padding: 0;
    margin: 0 0 2rem 0;
    text-align: left;
  }

  .rules-list li {
    font-size: 1.05rem;
    color: #334155;
    margin-bottom: 1.2rem;
    display: flex;
    gap: 0.75rem;
    line-height: 1.4;
  }

  .modal-btn {
    background: linear-gradient(135deg, #3b82f6 0%, #1d4ed8 100%); /* Degradado Azul */
    color: white;
    border: none;
    padding: 1rem 2.5rem;
    font-size: 1.2rem;
    font-weight: bold;
    border-radius: 999px;
    cursor: pointer;
    box-shadow: 0 8px 20px rgba(29, 78, 216, 0.4);
    transition: transform 0.2s ease, box-shadow 0.2s ease;
  }

  .modal-btn:hover {
    transform: translateY(-3px);
    box-shadow: 0 12px 25px rgba(29, 78, 216, 0.5);
  }

  /* Animaciones */
  .animate-pop {
    animation: popIn 0.4s cubic-bezier(0.34, 1.56, 0.64, 1) forwards;
  }

  .modal-fade-out {
    opacity: 0;
    visibility: hidden;
    transition: opacity 0.4s ease, visibility 0.4s ease;
  }

  @keyframes popIn {
    0% { transform: scale(0.7); opacity: 0; }
    100% { transform: scale(1); opacity: 1; }
  }

  /* --- TU CSS ORIGINAL (SE MANTIENE INTEGRO) --- */
  .game-container {
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: center;
    text-align: center;
    margin-top: 2rem;
    animation: fadeInUp 0.6s ease-out;
  }

  @keyframes fadeInUp {
    from { opacity: 0; transform: translateY(30px); }
    to { opacity: 1; transform: translateY(0); }
  }

  .header-beta {
    display: flex;
    justify-content: space-between;
    align-items: center;
    width: 100%;
    max-width: 600px;
    margin-bottom: 1.5rem;
  }

  .level-title {
    font-size: 2.2rem;
    margin: 0;
    text-transform: uppercase;
    letter-spacing: 3px;
    color: #f8fafc;
    font-weight: 800;
    text-shadow: 2px 2px 10px rgba(15, 23, 42, 0.18);
    filter: drop-shadow(1px 1px 4px rgba(15, 23, 42, 0.12));
  }

  .timer-box {
    background: linear-gradient(135deg, #EF4444, #F97316);
    color: #fff;
    padding: 0.6rem 1.2rem;
    border-radius: 16px;
    font-size: 1.3rem;
    font-weight: 700;
    font-family: 'Fredoka', monospace;
    box-shadow: 0 4px 15px rgba(239, 68, 68, 0.4);
    display: flex;
    align-items: center;
    gap: 0.3rem;
    transition: transform 0.2s ease;
  }

  :global(.timer-warning) {
    animation: timerPulse 0.5s ease-in-out infinite;
    background: linear-gradient(135deg, #DC2626, #EF4444) !important;
  }

  @keyframes timerPulse {
    0%, 100% { transform: scale(1); }
    50% { transform: scale(1.1); }
  }

  .screen {
    background: rgba(255, 255, 255, 0.22);
    backdrop-filter: blur(22px);
    color: #1e3a5f;
    padding: 2rem 4rem;
    border-radius: 24px;
    border: 1px solid rgba(255, 255, 255, 0.45);
    box-shadow: 
      0 16px 40px rgba(15, 23, 42, 0.12),
      inset 0 0 0 1px rgba(255, 255, 255, 0.55);
    margin-bottom: 2rem;
    min-width: 300px;
    position: relative;
  }

  .screen::before {
    content: '';
    position: absolute;
    inset: -2px;
    border-radius: 26px;
    background: rgba(255, 255, 255, 0.18);
    z-index: -1;
  }

  #math-question {
    font-size: 4rem;
    margin: 0;
    font-family: 'Fredoka', monospace;
    letter-spacing: 5px;
    color: #1e1b4b;
  }

  .fruits-container {
    display: flex;
    gap: 1.5rem;
    justify-content: center;
    flex-wrap: wrap;
  }

  :global(.fruit-btn) {
    background: rgba(255, 255, 255, 0.18);
    border: 1px solid rgba(255, 255, 255, 0.5);
    color: #ffffff;
    font-size: 2rem;
    font-weight: 800;
    padding: 1.4rem 2rem;
    border-radius: 28px;
    cursor: pointer;
    transition: transform 0.25s ease, box-shadow 0.25s ease, border-color 0.25s ease;
    min-width: 170px;
    box-shadow: 0 20px 40px rgba(0, 0, 0, 0.16);
    position: relative;
    overflow: hidden;
    backdrop-filter: blur(16px);
    text-shadow: 0 2px 8px rgba(0, 0, 0, 0.15);
  }

  :global(.fruit-btn::before) {
    content: '';
    position: absolute;
    inset: 0;
    background: linear-gradient(135deg, rgba(255,255,255,0.28) 0%, rgba(255,255,255,0.08) 40%, transparent 100%);
    pointer-events: none;
  }

  :global(.fruit-btn:hover) {
    transform: translateY(-10px) scale(1.03);
    box-shadow: 0 24px 50px rgba(0, 0, 0, 0.2);
  }

  :global(.fruit-btn.blue-card) {
    background: rgba(59, 130, 246, 0.24);
    border-color: rgba(59, 130, 246, 0.55);
  }

  :global(.fruit-btn.green-card) {
    background: rgba(16, 185, 129, 0.24);
    border-color: rgba(16, 185, 129, 0.55);
  }

  :global(.fruit-btn.red-card) {
    background: rgba(239, 68, 68, 0.24);
    border-color: rgba(239, 68, 68, 0.55);
  }

  :global(.fruit-btn.yellow-card) {
    background: rgba(234, 179, 8, 0.24);
    border-color: rgba(234, 179, 8, 0.55);
    color: #1f2937;
  }

  :global(.fruit-btn:active) {
    transform: translateY(-2px) scale(0.98);
  }

  .feedback {
    font-size: 1.5rem;
    font-weight: bold;
    margin-top: 2rem;
    min-height: 2rem;
  }

  :global(.correct) {
    color: #16a34a;
    animation: correctPop 0.5s cubic-bezier(0.34, 1.56, 0.64, 1);
  }

  :global(.incorrect) {
    color: #ef4444;
    animation: shake 0.5s ease;
  }

  @keyframes correctPop {
    0% { transform: scale(0.5); opacity: 0; }
    100% { transform: scale(1); opacity: 1; }
  }

  @keyframes shake {
    0%, 100% { transform: translateX(0); }
    25% { transform: translateX(-10px); }
    75% { transform: translateX(10px); }
  }
</style>

<script>
  //@ts-ignore
  import confetti from 'canvas-confetti';

  const questionEl = document.getElementById('math-question');
  const optionsContainer = document.getElementById('options-container');
  const feedbackEl = document.getElementById('feedback-message');
  const timeLeftEl = document.getElementById('time-left');
  const timerBox = document.querySelector('.timer-box');

  // --- VARIABLES DE CONTROL DEL MODAL (NUEVO) ---
  const rulesModal = document.getElementById('rules-modal');
  const btnCloseRules = document.getElementById('btn-close-rules');
  let modalAbierto = true; // Flag para impedir que corra el tiempo al inicio

  let timer: ReturnType<typeof setInterval> | undefined;
  let timeLeft = 15;
  let isAnswering = false;

  function startTimer() {
    if (modalAbierto) return; // Si las reglas están en pantalla, NO iniciamos el reloj
    
    timeLeft = 15;
    if (timeLeftEl) timeLeftEl.textContent = timeLeft.toString();
    if (timerBox) timerBox.classList.remove('timer-warning');
    clearInterval(timer);

    timer = setInterval(() => {
      timeLeft--;
      if (timeLeftEl) timeLeftEl.textContent = timeLeft.toString();

      if (timeLeft <= 5 && timerBox) {
        timerBox.classList.add('timer-warning');
      }

      if (timeLeft <= 0) {
        clearInterval(timer);
        handleTimeOut();
      }
    }, 1000);
  }

  function handleTimeOut() {
    isAnswering = true;
    if (feedbackEl) {
      feedbackEl.textContent = "¡Tiempo agotado!";
      feedbackEl.className = "feedback incorrect";
    }
    setTimeout(() => {
      generateQuestion();
    }, 2000);
  }

  function generateQuestion() {
    isAnswering = false;
    if (feedbackEl) feedbackEl.textContent = "";

    const isMultiplication = Math.random() > 0.5;
    let var1, var2, correctAnswer, operator;

    if (isMultiplication) {
      var1 = Math.floor(Math.random() * 10) + 2;
      var2 = Math.floor(Math.random() * 10) + 2;
      correctAnswer = var1 * var2;
      operator = '×';
    } else {
      var2 = Math.floor(Math.random() * 9) + 2;
      correctAnswer = Math.floor(Math.random() * 10) + 2;
      var1 = correctAnswer * var2;
      operator = '÷';
    }

    let wrong1 = correctAnswer + Math.floor(Math.random() * 4) + 1;
    let wrong2 = Math.max(1, correctAnswer - (Math.floor(Math.random() * 4) + 1));

    if (wrong1 === wrong2) wrong1 += 1;
    if (wrong1 === correctAnswer) wrong1 += 2;
    if (wrong2 === correctAnswer) wrong2 -= 1;

    const options = [
      { value: correctAnswer, isCorrect: true },
      { value: wrong1, isCorrect: false },
      { value: wrong2, isCorrect: false }
    ];

    options.sort(() => Math.random() - 0.5);

    if (questionEl) {
      questionEl.textContent = `${var1} ${operator} ${var2} = ?`;
    }

    renderOptions(options);
    startTimer();
  }

  function renderOptions(options: { value: number; isCorrect: boolean }[]) {
    if (!optionsContainer) return;
    optionsContainer.innerHTML = '';

    const labels = ['A', 'B', 'C'];
    const colorClasses = ['blue-card', 'green-card', 'red-card', 'yellow-card'];

    options.forEach((opt, index) => {
      const btn = document.createElement('button');
      btn.className = `fruit-btn ${colorClasses[index % colorClasses.length]}`;
      btn.textContent = `Opción ${labels[index]}: ${opt.value}`;

      btn.addEventListener('click', () => {
        if (!isAnswering) checkAnswer(opt.isCorrect);
      });

      optionsContainer.appendChild(btn);
    });
  }

  function checkAnswer(isCorrect: boolean) {
    isAnswering = true;
    clearInterval(timer);

    if (!feedbackEl || !optionsContainer) return;

    if (isCorrect) {
      feedbackEl.textContent = "¡Correcto!";
      feedbackEl.className = "feedback correct";

      const audioGanar = new Audio('/sounds/Ganar.mp3');
      audioGanar.play();

      confetti({
        particleCount: 150,
        spread: 80,
        origin: { y: 0.6 }
      });

      optionsContainer.style.display = 'none'; 
      document.dispatchEvent(new CustomEvent('respuestaCorrecta'));

    } else {
      feedbackEl.textContent = "Intenta de nuevo";
      feedbackEl.className = "feedback incorrect";

      const audioPerder = new Audio('/sounds/perder.mp3');
      audioPerder.play();

      for (let i = 0; i < 10; i++) {
        const x = document.createElement('div');
        x.textContent = '❌'; 
        x.style.position = 'fixed';
        x.style.top = '-50px';
        x.style.left = Math.random() * 100 + 'vw'; 
        x.style.fontSize = Math.random() * (30 - 20) + 20 + 'px';
        x.style.zIndex = '9999';
        x.style.pointerEvents = 'none';
        x.style.transition = 'transform 1s linear, opacity 1s ease';
        
        document.body.appendChild(x);

        setTimeout(() => {
          x.style.transform = `translateY(110vh) rotate(${Math.random() * 360}deg)`;
          x.style.opacity = '0';
          setTimeout(() => x.remove(), 1000);
        }, Math.random() * 200);
      }
  
      setTimeout(() => {
        generateQuestion();
      }, 1500);
    }
  }

  // Lógica para cerrar el Modal e iniciar formalmente el conteo regresivo
  if (btnCloseRules && rulesModal) {
    btnCloseRules.addEventListener('click', () => {
      rulesModal.classList.add('modal-fade-out');
      setTimeout(() => {
        rulesModal.style.display = 'none';
        modalAbierto = false; // Desbloquea el reloj
        startTimer();        // Activa los 15 segundos ahora sí
      }, 400);
    });
  }

  document.addEventListener('siguienteTurno', () => {
    if (feedbackEl) feedbackEl.textContent = "";
    if (optionsContainer) optionsContainer.style.display = 'flex'; 
    generateQuestion(); 
  });

  document.addEventListener('avanzarFicha', (e: any) => {
    console.log(`Beta: El jugador saca dado de ${e.detail.pasos}. Mover ficha.`);
  });

  // Genera la pregunta base (las respuestas se pintan pero el reloj esperará el clic del modal)
  generateQuestion();
</script>